# Copy to .env and fill in after creating the project (https://www.sanity.io/manage
# or `npx sanity init`). The project id is public - it appears in every API URL.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
